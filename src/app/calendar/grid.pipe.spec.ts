/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GridPipe } from './grid.pipe';

describe('GridPipe', () => {
  it('create an instance', () => {
    const pipe = new GridPipe();
    expect(pipe).toBeTruthy();
  });
});
